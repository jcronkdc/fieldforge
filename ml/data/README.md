# Data Directory

- `raw/` – Original story seeds, transcripts, or dataset exports (gitignored).
- `processed/` – Cleaned or feature-engineered datasets ready for modeling (gitignored).
- `samples/` – Lightweight illustrative texts safe to commit for tests.

Document the source, license, and transformations for each dataset you introduce.

